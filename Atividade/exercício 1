 int[] v = new int[10];
            for ( int i=0; i<10; i++)
            {
                v[i] = int.Parse(Console.ReadLine());
            }
            Console.WriteLine("os numeros negativos são: ");
            for(int i = 0; i<10;i++)
            {
                if(v[i] < 0)
               
                Console.WriteLine(v[i]);
            }
